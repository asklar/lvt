# Build output
build/
build-*/
_stage/
packages/
# Managed (.NET) build output, emitted next to every .csproj — the sample apps,
# the WPF/WinForms TAP assemblies, and the Avalonia tree walker.
bin/
obj/

# Cargo output for the MCP server. The CMake build redirects this into build/
# via CARGO_TARGET_DIR, but a bare `cargo test` in mcp/ still creates it here.
mcp/target/

# IDE files
.vs/
.vscode/
*.user
CMakeSettings.json

# OS files
Thumbs.db
Desktop.ini
.DS_Store
*.png
!docs/*.png
_codeql_detected_source_root

src/tap/winui3/
