<Window
    x:Class="WinUI3Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="LVT WinUI3 Sample">

    <Grid x:Name="RootGrid" Padding="24" RowSpacing="12" Width="460" Height="760">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" Text="LVT WinUI3 Sample" FontSize="24" />
        <TextBox x:Name="InputBox" Grid.Row="1" Header="Sample input" Text="Durable key text" />
        <TextBox x:Name="ReadOnlyBox" Grid.Row="2" Header="Read-only input"
                 Text="Cannot edit" IsReadOnly="True" />
        <TextBox x:Name="RawOnlyInput" Grid.Row="3" Header="Raw-view input"
                 Text="raw value" AutomationProperties.AccessibilityView="Raw" />
        <Button x:Name="PrimaryButton" Grid.Row="4" Content="Primary action" Click="OnPrimaryClick" />
        <CheckBox x:Name="ReadyCheckBox" Grid.Row="5" Content="Ready" IsChecked="True" />
        <!-- Gives interaction tests something observable to assert against:
             a click has no effect a UIA walk can see unless it changes state. -->
        <TextBlock x:Name="StatusText" Grid.Row="6" Text="clicks:0"
                   TextAlignment="Left" ManipulationMode="TranslateX"
                   TextDecorations="None" />

        <!-- The controls below exist so the interaction verbs have something to
             act on: a slider for RangeValue, a combo box for ExpandCollapse and
             Selection, and a long virtualized list for Scroll, SelectionItem
             and VirtualizedItem.Realize. -->
        <Slider x:Name="LevelSlider" Grid.Row="7" Minimum="0" Maximum="100" Value="25" />
        <Slider x:Name="ReadOnlyRange" Grid.Row="8"
                Minimum="0" Maximum="100" Value="40" IsEnabled="False" />
        <StackPanel Grid.Row="9" Orientation="Horizontal" Spacing="12">
            <ComboBox x:Name="ChoiceCombo" SelectedIndex="0" Width="200">
                <ComboBoxItem Content="Alpha" />
                <ComboBoxItem Content="Beta" />
                <ComboBoxItem Content="Gamma" />
            </ComboBox>
            <Button x:Name="ToggleListSizeButton"
                    Content="Toggle list size"
                    Click="OnToggleListSizeClick" />
            <Button x:Name="ReorderListButton"
                    Content="Reorder"
                    Click="OnReorderListClick" />
        </StackPanel>
        <ListView x:Name="ItemsList" Grid.Row="10" Height="220" SelectionMode="Multiple" />
    </Grid>
</Window>
